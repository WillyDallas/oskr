
# Secrets — never commit (the plugin is stateless; tokens live in the workspace)
.env
*.env
*.key
*.pem

# macOS
.DS_Store

# Runtime / working artifacts
logs/
docs/temp/
docs/_local_archive/

# Consumer projects managed by oskr (separate git repos)
repos/*
!repos/.gitkeep
!repos/projects.example.json
